## 🛠️ Technical Progression

I have completed the following hands-on Git exercises to simulate real-world development environments:

### 1. Merge Conflict Resolution
* **Scenario:** Created a functional "collision" by editing the same line in `index.html` across two different branches (`main` and `branch1`).
* **Action:** Successfully performed a manual merge, identified conflict markers (`<<<<<<<`, `=======`, `>>>>>>>`), and resolved the code logic.
* **Outcome:** Learned to handle complex code integration and maintain version integrity.

### 2. Pull Request (PR) Lifecycle
* **Scenario:** Simulated a team collaboration workflow.
* **Action:** * Created a feature branch (`branchPR`).
    * Pushed changes to GitHub and opened a formal Pull Request.
    * Conducted a self-review and successfully merged the changes into the `main` branch.
* **Outcome:** Gained experience with GitHub’s code review interface and repository management.